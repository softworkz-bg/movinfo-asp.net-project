@model MovieViewModel
<div class="moviesResultsScroll">
    @for (int i = 0; i < Model.AllMoviesIdsAndNames.Count; i++)
    {
        var item = @Model.AllMoviesIdsAndNames.ElementAt(i);

        <p class="btn-movie" onclick="addMovie(event)">@item.Key @item.Value</p>
    }
</div>  